# DishGinee Workspace Rules

## Git Workflow Rule
- Whenever code changes are made and confirmed by the user, stage (`git add`), commit with a descriptive message, and push (`git push`) to the repository.
